#!/bin/bash
pgrep -x sxhkd > /dev/null || sxhkd &

~/.config/polybar/launch.sh &

if [[ $(hostname) == "kucing" ]]
then
	bspc monitor DVI-I-1 -d I II III IV V
	bspc monitor HDMI-1 -d VI VII VIII IX X
else
	bspc monitor eDP-1 -d I II III IV V VI VII VIII IX X
fi

bspc rule -a Emacs state=tiled #for some reason, emacs likes to start up floating

xsetroot -cursor_name left_ptr &

#starting some Nice Programs
bspc rule -a URxvt -o desktop='^1'; urxvt &
bspc rule -a qutebrowser -o desktop='^2'; qutebrowser &
bspc rule -a Emacs -o desktop='^3'; emacs &
