super + shift + Return
      urxvt

super + o
      rofi -show run

super +{_,shift +} q
      bspc node {-k,-c}

super + {_,shift +}{1-9,0}
      bspc {desktop -f,node -d} '^{1-9,10}'

XF86Audio{Raise,Lower}Volume
	pactl set-sink-volume {+,-}5%
